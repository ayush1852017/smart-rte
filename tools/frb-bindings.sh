#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR=$(cd "$(dirname "$0")/.." && pwd)

echo "Generating flutter_rust_bridge bindings..."
cd "$ROOT_DIR"
echo "(placeholder) Run flutter_rust_bridge_codegen here"

echo "Done."


