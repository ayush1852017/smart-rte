// Legacy compatibility entry point. The canonical implementation lives inside
// the foundation boundary so mark commands do not depend on root contracts.
export * from "../foundation/security/urlPolicy.js";
