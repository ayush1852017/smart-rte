# Captured P0 clipboard fixtures

Only verbatim clipboard payloads captured from the real source application belong here. Follow `docs/PHASE8A_FIXTURE_CAPTURE.md`.

Synthesized security, property, and stress fixtures belong in separate test-fixture directories and must never be described as source-application captures.
