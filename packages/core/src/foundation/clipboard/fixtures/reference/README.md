# Clipboard reference fixtures

These files are converted references, not raw clipboard captures. They may be
used for parser hardening but must never be counted as captured-source evidence.

DOCX references are generated through Mammoth and carry
`provenance.kind: "docx-reference"`. A DOCX package does not contain the HTML
emitted by Word's Windows or macOS clipboard implementation.
